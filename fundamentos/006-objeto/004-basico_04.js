const usuario = {
    email:'vinicius@123.com',
};

usuario.nome = 'Vinicius'; // posso declarar atributos fora do objeto dessa forma
usuario.senha = '********';
usuario.senha = 10;

console.log(usuario.email, usuario.nome);
console.log(usuario);
